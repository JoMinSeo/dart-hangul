name: dart_hangul
description: Korean Hangul utilities for Dart - jamo disassembly/assembly, josa (particle) selection, batchim/choseong extraction, and number-to-Hangul conversion.
version: 0.2.0
repository: https://github.com/JoMinSeo/dart-hangul

environment:
  sdk: ^3.7.2

dependencies:
  unorm_dart: ^0.3.2
dev_dependencies:
  lints: ^5.1.1
  test: ^1.31.0
